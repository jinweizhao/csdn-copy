import 'package:flutter/cupertino.dart';

class BlinkScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('blink screen'),
      ),
    );
  }
}